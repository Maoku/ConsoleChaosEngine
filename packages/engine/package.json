{
  "name": "@console-chaos/engine",
  "version": "0.2.0",
  "description": "Generation-aware browser game engine with deterministic simulation, input, audio, assets, and WebGL rendering.",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "RELEASE_NOTES.md"
  ],
  "keywords": [
    "game-engine",
    "typescript",
    "webgl",
    "retro"
  ],
  "scripts": {
    "build": "vite build --config vite.config.package.ts && tsc -p tsconfig.build.json --emitDeclarationOnly && node ../../tools/fix-declaration-specifiers.mjs dist",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "prepack": "npm run build"
  },
  "dependencies": {
    "gl-matrix": "^3.4.3"
  }
}
